node_modules/
*.log
graphify-out/

# Certs de mkcert para probar HTTPS local (ver README) y el .env opcional
# que define HTTPS_LOCAL_ORIGIN para docker-compose.https.yml. Se generan
# por developer/red, nunca se commitean.
certs/
.env
